name: WordPress Plugin Check

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  plugin-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Build production tree
        shell: bash
        run: |
          mkdir plugin-build
          git archive HEAD | tar -x -C plugin-build

      - name: Lint PHP
        shell: bash
        run: find plugin-build -name '*.php' -print0 | xargs -0 -n1 php -l

      - name: Run Plugin Check
        uses: wordpress/plugin-check-action@v1
        with:
          build-dir: ./plugin-build
